# OrbStack により追加: Linux マシン用の 'orb' SSH ホスト設定
# この設定は ssh_config の先頭（Host ブロックより前）にある場合のみ機能します
# 削除した場合でも再度自動追加はされません
Include ~/.orbstack/ssh/config

Host github.com
  IdentitiesOnly yes
  User git
  # Bitwarden SSH Agent 移行時に以下は削除すること
  AddKeysToAgent yes
  IdentityFile ~/.ssh/id_ed25519_github
